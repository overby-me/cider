# CI for Cider.
#
# THIS FILE WAS DISCONNECTED IN TWO INDEPENDENT WAYS UNTIL 2026-08-12, and that is why two
# breaks in the flake DEFAULT output sat unnoticed until someone evaluated it by hand:
#
#   it triggered on `main` only, and main is 22 tasks behind the branch the work is on, so
#     it had never run on any of it;
#   it built .#cider and .#cider-sdk, and NEITHER was defined in flake.nix, so even if it had
#     fired it would have failed on a missing attribute.
#
# .#cider exists now (it is the product). .#cider-sdk never came back: there is no SDK output
# in this flake, only cider-buck2-probe-sdkenv, which is a probe, so asking for it would be
# asking for something that was never there.
#
# THE ORDER BELOW IS DELIBERATE: cheapest first, and each step is allowed to be the one that
# fails. Evaluation alone would have caught both of the breaks above, and it costs minutes
# rather than hours, so it runs before anything is built.
when:
  - event: ["push", "pull_request"]
    branch: ["main", "buck2-port"]

engine: microvm
image: nixos

# A package list rather than an install step: the engine puts these in every step, and caches
# them, which is the whole reason to be on this engine rather than the nixery one.
dependencies:
  - git

# THE FEATURES GO IN THE ENVIRONMENT RATHER THAN ON THE COMMAND, and the difference is not
# cosmetic. Naming them with --extra-experimental-features configures THAT nix invocation, and the
# run still died on
#
#   error: experimental Nix feature 'ca-derivations' is disabled
#
# because evaluating this flake starts further nix processes, which inherit the environment and not
# the argument list. NIX_CONFIG reaches all of them.
environment:
  NIX_CONFIG: "experimental-features = nix-command flakes ca-derivations"
  # AND THE DAEMON HAS TO BE OUT OF THE WAY, which NIX_CONFIG alone does not achieve. Setting the
  # features in the environment reached every nix process and the run STILL died on the same line,
  # because experimental features governing store operations are a TRUSTED setting: the daemon
  # decides, and a client cannot talk it into anything. An empty NIX_REMOTE makes nix work on the
  # store directly instead of asking a daemon, which is allowed here because each workflow gets its
  # own microVM and runs as root in it.
  NIX_REMOTE: ""

steps:
  # THERE IS NO CACHIX STEP, because there is no cachix cache. This file asked for one named
  # cider-nix from the day it was written, and the first run that ever reported a result answered
  #
  #   Binary cache cider-nix doesn't exist or it's private and you need a token
  #
  # in 0s, which failed the workflow before it reached the evaluation the file exists to do. That
  # is not an engine problem and moving engines did not cause it: the old file ran the same
  # `cachix use` and would have died on the same line. The name was aspirational rather than real.
  #
  # Evaluation is also the step that needs a cache least, since it is allowed to build nothing.
  # If a cache is created later it belongs in the microVM `caches` field, which wants the trusted
  # public key next to the URL.

  # EVERY ADVERTISED OUTPUT MUST EVALUATE. This is the step that would have caught both 2026-08-12
  # breaks: one died in buck2 analysis and one on a builtins.readDir of a directory #87 deleted,
  # and neither needed a single compile to surface. `nix flake show` walks the outputs, and
  # --all-systems is off because this flake is x86_64-linux only until the aarch64 release.
  #
  # NOT `nix flake show`, AND THAT IS THE WHOLE POINT OF THIS STEP.
  #
  # With the daemon out of the way the evaluation finally ran, walked checks.x86_64-linux one by one
  # and died on packages-cider-buck2-all. Reproduced locally, that failure is not a broken output at
  # all: this flake uses IMPORT FROM DERIVATION, so evaluating that attribute has to BUILD
  # materialize-pins first. `nix flake show` therefore cannot finish without building, which
  # contradicts the premise this file is written on and stated below, that CI never builds.
  #
  # So the outputs are listed instead of shown. This still catches what motivated the file, an
  # advertised attribute that does not exist, and it completes in seconds without building anything.
  # WHAT IT DOES NOT CATCH, said plainly rather than left to be discovered: attrNames does not force
  # the values, so a package whose definition is broken inside still passes here. Catching that needs
  # a builder, which is a developer machine rather than a hosted runner.
  - name: "Flake outputs exist"
    command: |
      for out in packages checks devShells; do
        echo "--- $out"
        nix eval --json ".#$out.x86_64-linux" --apply builtins.attrNames
      done

  # NO BUILD STEPS, AND THAT IS THE POINT RATHER THAN A GAP. Darling is too large for a hosted
  # runner to finish; a CI that attempts it is a CI that always fails and is therefore ignored,
  # which is worse than one that checks less and is believed. Building is what a developer
  # machine is for.
  #
  # What survives is the half that is cheap AND caught the real bugs: both breaks that motivated
  # this file, a buck2 analysis failure and a builtins.readDir of a directory #87 deleted, were
  # EVAL errors. Neither needed a compile. The step above would have caught both in seconds.
