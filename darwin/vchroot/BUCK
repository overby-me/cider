load("//buck/rules:cc.bzl", "cc_objects")
load("//buck/rules:darwin.bzl", "darwin_binary")

# BEGIN generated: vchroot
# GENERATED from the reference build.ninja by
# gen-buck-from-ninja (deleted) vchroot   -- review before committing.
# cmake target: vchroot  ->  src/vchroot/vchroot

cc_objects(
    name = "vchroot_obj",
    srcs = [
        "vchroot.c",
    ],
    compiler_flags = [
        "-nostdinc",
    ],
    toolchain = "toolchains//:darwin_cc",
    deps = [
        "//darwin:sdk_env",
    ],
    visibility = ["PUBLIC"],
)
# END generated: vchroot

# BEGIN generated: vchroot binary
# GENERATED from the reference build.ninja by
# gen-buck-from-ninja (deleted) --binaries vchroot   -- review before committing.
# Links 1 cmake object library/ies, 2 flag groups in total.
# TODO it also links these libraries, not ported yet:
#   libcrt1.10.6.a

darwin_binary(
    name = "vchroot",
    objs = [
        # vchroot
        ":vchroot_obj",
        # crt1.10.6
        "//buck-src/csu:crt1.10.6_obj2",
    ],
    dylibs = [
        "//buck-src:system_final",
    ],
    toolchain = "toolchains//:darwin_cc",
    deps = [
        "//darwin:sdk_env",
    ],
    visibility = ["PUBLIC"],
)
# END generated: vchroot binary
