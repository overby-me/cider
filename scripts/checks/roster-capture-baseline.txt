# Byte size of captures/sweep-<tag>/d1-start.png for a known good build.
#
# WHY THIS EXISTS. capture-is-black.py answers one question, whether anything was drawn, and a
# window that loses its entire file list still answers CONTENT. That happened: an attempt at stack
# view sizing emptied the iA Writer library column and the sweep passed 7 of 7 without a murmur.
#
# A grim capture of identical pixels is byte identical, measured across many runs of this session,
# so a changed size means the rendering changed and the capture wants looking at. It does not mean
# failure: most changes here have been improvements.
#
# THE TOLERANCE IS MEASURED, not guessed. cm alternates between 35846 and 35854 across runs, a
# jitter of 8 bytes, while a real regression moves thousands. 256 absorbs the first and catches the
# second.
#
# Refresh with ROSTER_BASELINE_UPDATE=1 scripts/roster-sweep.sh after looking at every capture.
#
# cm MOVED AGAIN from 33544 to 34456 on 2026-09-23, and this time it is an IMPROVEMENT, not residue.
# cocotron 0134 fixed the glyph position rule, so every descender that CMake had been drawing three
# rows too low now renders in place: Configure, update, display, press, Generate and Grouped all
# read correctly where they used to read ConfiQure, uDdate, disDlav, Dress, Qenerate and GrouDed.
# More ink is more bytes. See docs/cmake-descenders.md.
#
# cm MOVED from 35850 to 33544 on 2026-09-23, and the move was mine. CMake persists its settings in
# Users/root/Library/Preferences, so driving it through a real Configure left the source path, the
# generator and five red cache entries behind, and the NEXT startup capture showed all of them. The
# old number had the same shape of residue in it from an earlier session: a remembered build
# directory in the title bar. 33544 is CMake with its preferences and the stale build tree removed,
# which is the state a fresh machine produces, so it is the one worth comparing against.
ia 31129
sp 49610
mm 15860
it 16578
lo 137276
mx 32124
cm 34456
