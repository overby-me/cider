diff -u --recursive a/AppKit/include/AppKit/NSWorkspace.h b/AppKit/include/AppKit/NSWorkspace.h
--- a/AppKit/include/AppKit/NSWorkspace.h	2026-08-13 21:58:41.255308297 +0200
+++ b/AppKit/include/AppKit/NSWorkspace.h	2026-08-13 21:58:41.291793452 +0200
@@ -58,6 +58,12 @@
 
 - (NSNotificationCenter *) notificationCenter;
 
+- (BOOL) accessibilityDisplayShouldReduceMotion;
+- (BOOL) accessibilityDisplayShouldReduceTransparency;
+- (BOOL) accessibilityDisplayShouldIncreaseContrast;
+- (BOOL) accessibilityDisplayShouldDifferentiateWithoutColor;
+- (BOOL) accessibilityDisplayShouldInvertColors;
+
 - (NSImage *) iconForFile: (NSString *) path;
 - (NSImage *) iconForFiles: (NSArray *) array;
 - (NSImage *) iconForFileType: (NSString *) type;
diff -u --recursive a/AppKit/NSWorkspace.m b/AppKit/NSWorkspace.m
--- a/AppKit/NSWorkspace.m	2026-08-13 21:58:41.253308282 +0200
+++ b/AppKit/NSWorkspace.m	2026-08-13 21:58:41.284329785 +0200
@@ -69,6 +69,37 @@
 
 @implementation NSWorkspace
 
+/*
+ * The accessibility display preferences, 10.10 and later.
+ *
+ * ALL FOUR, NOT JUST THE ONE THAT ABORTED. They are queried together by anything that adapts its
+ * drawing, so implementing them one at a time costs a container run each to learn the next name;
+ * LibreOffice asked for ShouldReduceMotion and would have asked for its siblings next.
+ *
+ * NO is the truthful answer rather than a placeholder: these report USER PREFERENCES, and there
+ * is no preferences database here to have set one. A Mac with default settings answers exactly
+ * this.
+ */
+- (BOOL) accessibilityDisplayShouldReduceMotion {
+    return NO;
+}
+
+- (BOOL) accessibilityDisplayShouldReduceTransparency {
+    return NO;
+}
+
+- (BOOL) accessibilityDisplayShouldIncreaseContrast {
+    return NO;
+}
+
+- (BOOL) accessibilityDisplayShouldDifferentiateWithoutColor {
+    return NO;
+}
+
+- (BOOL) accessibilityDisplayShouldInvertColors {
+    return NO;
+}
+
 + (NSWorkspace *) sharedWorkspace {
     return NSThreadSharedInstance(@"NSWorkspace");
 }
