--- a/src/NSXPCConnection.m
+++ b/src/NSXPCConnection.m
@@ -1,3 +1,5 @@
+#include <stdio.h>
+#include <stdlib.h>
 /*
  This file is part of Darling.
 
@@ -604,6 +606,18 @@
                         [NSException raise: NSInvalidArgumentException format: @"Reply block for %s was nil", sel_getName(selector)];
                     }
 
+                    /* CIDER_TRACE_XPC: _Block_signature crashed here reading a pointer that is
+                     * not a block, and the nil check above cannot catch that. Say what the
+                     * signature claims and what was actually fetched, since a misread parameter
+                     * type turns an integer argument into a block. */
+                    if (getenv("CIDER_TRACE_XPC") != NULL) {
+                        fprintf(stderr, "CIDER_XPCSEND sel=%s params=%lu idx=%lu type=%s block=%p frameLen=%lu\n",
+                                sel_getName(selector), (unsigned long) parameterCount,
+                                (unsigned long) (i + 2), paramType, (void *) block,
+                                (unsigned long) [signature frameLength]);
+                        fflush(stderr);
+                        fflush(stderr);
+                    }
                     rawBlockSig = _Block_signature(block);
                     if (!rawBlockSig) {
                         [NSException raise: NSInvalidArgumentException format: @"Reply block for %s was compiled without an embedded signature", sel_getName(selector)];
